<div id="menu">
<div id="black" ></div>
	<ul class="menu">
		<li><%= link_to "meals", user_path(@user.id) %></li>
		<% if current_user %>
		  <% if current_user.id == @user.id %>
		    <%= link_to("new meal", new_meal_path) %>
		  <%end%>
		<%end%>
	</ul>
</div>