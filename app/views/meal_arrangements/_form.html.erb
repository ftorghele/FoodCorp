<%= form_for(@meal_arrangement) do |f| %>
  <% if @meal_arrangement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_arrangement.errors.count, "error") %> prohibited this meal_arrangement from being saved:</h2>

      <ul>
      <% @meal_arrangement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meal_id %><br />
    <%= f.text_field :meal_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
